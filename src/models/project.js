export default class Project {
  constructor(name, description, url, githubUrl, stack) {
    this.name = name;
    this.description = description;
    this.url = url;
    this.githubUrl = githubUrl;
    this.stack = stack;
  }
}
